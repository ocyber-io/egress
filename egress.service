[Unit]
Wants=network.target
[Service]
Environment="PATH=$PATH:/usr/local/bin/egress"
WorkingDirectory=/usr/local/bin
ExecStart=/usr/local/bin/egress --config /usr/local/etc/egress.yaml
Restart=always
[Install]
WantedBy=multi-user.target
